"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.EscalationChain = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("../utilities");
/**
 * * [HTTP API](https://grafana.com/docs/oncall/latest/oncall-api-reference/escalation_chains/)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const myTeam = grafana.oss.getTeam({
 *     name: "my team",
 * });
 * const myTeamGetTeam = myTeam.then(myTeam => grafana.onCall.getTeam({
 *     name: myTeam.name,
 * }));
 * const _default = new grafana.oncall.EscalationChain("default", {
 *     name: "default",
 *     teamId: myTeamGetTeam.then(myTeamGetTeam => myTeamGetTeam.id),
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import grafana:onCall/escalationChain:EscalationChain name "{{ id }}"
 * ```
 */
class EscalationChain extends pulumi.CustomResource {
    /**
     * Get an existing EscalationChain resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new EscalationChain(name, state, { ...opts, id: id });
    }
    /**
     * Returns true if the given object is an instance of EscalationChain.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EscalationChain.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["name"] = state?.name;
            resourceInputs["teamId"] = state?.teamId;
        }
        else {
            const args = argsOrState;
            resourceInputs["name"] = args?.name;
            resourceInputs["teamId"] = args?.teamId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EscalationChain.__pulumiType, name, resourceInputs, opts);
    }
}
exports.EscalationChain = EscalationChain;
/** @internal */
EscalationChain.__pulumiType = 'grafana:onCall/escalationChain:EscalationChain';
//# sourceMappingURL=escalationChain.js.map