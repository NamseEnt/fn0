"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.PublicIpPoolCapacity = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("../utilities");
/**
 * This resource is used to manage the `cidrBlocks` of Public Ip Pool resource in Oracle Cloud Infrastructure Core service.
 * Adds a Cidr from the named Byoip Range prefix to the referenced Public IP Pool. The cidr must be a subset of the Byoip Range in question. The cidr must not overlap with any other cidr already added to this or any other Public Ip Pool.
 *
 * **Note:** When a new `oci.Core.PublicIpPoolCapacity` resource is created or removed, terraform needs to be refreshed to update the `cidrBlocks` of `oci.Core.PublicIpPool` resource in state file.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as oci from "@pulumi/oci";
 *
 * const testPublicIpPoolCapacity = new oci.core.PublicIpPoolCapacity("test_public_ip_pool_capacity", {
 *     publicIpPoolId: publicIpPoolId,
 *     byoipId: byoipId,
 *     cidrBlock: cidrBlock,
 * });
 * ```
 *
 * ## Import
 *
 * PublicIpPoolCapacity can be imported using the `id`, e.g.
 *
 * ```sh
 * $ pulumi import oci:Core/publicIpPoolCapacity:PublicIpPoolCapacity test_public_ip_pool_capacity "publicIpPoolId/{publicIpPoolId}/byoipId/{byoipId}/cidrBlock/{cidrBlock}"
 * ```
 */
class PublicIpPoolCapacity extends pulumi.CustomResource {
    /**
     * Get an existing PublicIpPoolCapacity resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new PublicIpPoolCapacity(name, state, { ...opts, id: id });
    }
    /**
     * Returns true if the given object is an instance of PublicIpPoolCapacity.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === PublicIpPoolCapacity.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["byoipId"] = state?.byoipId;
            resourceInputs["cidrBlock"] = state?.cidrBlock;
            resourceInputs["publicIpPoolId"] = state?.publicIpPoolId;
        }
        else {
            const args = argsOrState;
            if (args?.byoipId === undefined && !opts.urn) {
                throw new Error("Missing required property 'byoipId'");
            }
            if (args?.cidrBlock === undefined && !opts.urn) {
                throw new Error("Missing required property 'cidrBlock'");
            }
            if (args?.publicIpPoolId === undefined && !opts.urn) {
                throw new Error("Missing required property 'publicIpPoolId'");
            }
            resourceInputs["byoipId"] = args?.byoipId;
            resourceInputs["cidrBlock"] = args?.cidrBlock;
            resourceInputs["publicIpPoolId"] = args?.publicIpPoolId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(PublicIpPoolCapacity.__pulumiType, name, resourceInputs, opts);
    }
}
exports.PublicIpPoolCapacity = PublicIpPoolCapacity;
/** @internal */
PublicIpPoolCapacity.__pulumiType = 'oci:Core/publicIpPoolCapacity:PublicIpPoolCapacity';
//# sourceMappingURL=publicIpPoolCapacity.js.map