"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.ZonePromoteDnssecKeyVersion = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("../utilities");
/**
 * This resource provides the Zone Promote Dnssec Key Version resource in Oracle Cloud Infrastructure DNS service.
 * Api doc link for the resource: https://docs.oracle.com/iaas/api/#/en/dns/latest/ZonePromoteDnssecKeyVersion
 *
 * Example terraform configs related to the resource : https://github.com/oracle/terraform-provider-oci/tree/master/examples/dns
 *
 * Promotes a specified `DnssecKeyVersion` on the zone.
 *
 * If the `DnssecKeyVersion` identified in the request body is a key signing key (KSK) that is replacing
 * another `DnssecKeyVersion`, then the old `DnssecKeyVersion` is scheduled for removal from the zone.
 *
 * For key signing keys (KSKs), you must create the DS record with the new key information **before** promoting
 * the new key to establish a chain of trust. To avoid a service disruption, remove the old DS record as soon
 * as its TTL (time to live) expires.
 *
 * For more information, see [DNSSEC](https://docs.cloud.oracle.com/iaas/Content/DNS/Concepts/dnssec.htm).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as oci from "@pulumi/oci";
 *
 * const testZonePromoteDnssecKeyVersion = new oci.dns.ZonePromoteDnssecKeyVersion("test_zone_promote_dnssec_key_version", {
 *     dnssecKeyVersionUuid: zonePromoteDnssecKeyVersionDnssecKeyVersionUuid,
 *     zoneId: testZone.id,
 *     scope: zonePromoteDnssecKeyVersionScope,
 * });
 * ```
 *
 * ## Import
 *
 * Import is not supported for this resource.
 */
class ZonePromoteDnssecKeyVersion extends pulumi.CustomResource {
    /**
     * Get an existing ZonePromoteDnssecKeyVersion resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new ZonePromoteDnssecKeyVersion(name, state, { ...opts, id: id });
    }
    /**
     * Returns true if the given object is an instance of ZonePromoteDnssecKeyVersion.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ZonePromoteDnssecKeyVersion.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["dnssecKeyVersionUuid"] = state?.dnssecKeyVersionUuid;
            resourceInputs["scope"] = state?.scope;
            resourceInputs["zoneId"] = state?.zoneId;
        }
        else {
            const args = argsOrState;
            if (args?.dnssecKeyVersionUuid === undefined && !opts.urn) {
                throw new Error("Missing required property 'dnssecKeyVersionUuid'");
            }
            if (args?.zoneId === undefined && !opts.urn) {
                throw new Error("Missing required property 'zoneId'");
            }
            resourceInputs["dnssecKeyVersionUuid"] = args?.dnssecKeyVersionUuid;
            resourceInputs["scope"] = args?.scope;
            resourceInputs["zoneId"] = args?.zoneId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ZonePromoteDnssecKeyVersion.__pulumiType, name, resourceInputs, opts);
    }
}
exports.ZonePromoteDnssecKeyVersion = ZonePromoteDnssecKeyVersion;
/** @internal */
ZonePromoteDnssecKeyVersion.__pulumiType = 'oci:Dns/zonePromoteDnssecKeyVersion:ZonePromoteDnssecKeyVersion';
//# sourceMappingURL=zonePromoteDnssecKeyVersion.js.map