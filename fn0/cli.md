User can set what server-component will be used in fn0.

For example, user can select local dev mode, then user should put wasm path to serve it with localhost:3000 without code id.

`fn0 local {wasm path}` --> local run. port is start on 3000, but if failed to open, try 3001 and so on.
