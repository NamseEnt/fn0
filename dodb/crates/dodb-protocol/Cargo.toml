[package]
name = "dodb-protocol"
version = "0.1.0"
edition.workspace = true
license = "MIT"

[dependencies]
dodb-core = { path = "../dodb-core" }
dodb-service = { path = "../dodb-service" }

[dev-dependencies]
proptest.workspace = true
