[package]
name = "dodb-testkit"
version = "0.1.0"
edition.workspace = true
license = "MIT"

[dependencies]
dodb-core = { path = "../dodb-core" }
dodb-storage = { path = "../dodb-storage" }

[dev-dependencies]
proptest.workspace = true

