[package]
name = "dodb-testkit"
version = "0.1.0"
edition.workspace = true
license = "MIT"

[package.metadata.release]
release = false

[dependencies]
dodb-core.workspace = true
dodb-storage.workspace = true

[dev-dependencies]
proptest.workspace = true
tokio.workspace = true
