[package]
name = "dodb-core"
version = "0.1.0"
edition.workspace = true
license = "MIT"
description = "Core types for dodb, the fn0 document database"
repository.workspace = true

[dev-dependencies]
proptest.workspace = true

